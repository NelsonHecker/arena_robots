prefix: ""
mounts:
  lidar:
    parent: base
    xyz: [0, 0, 0.3]
    rpy: [0, 0, 0]
    accepts: [lidar]
  imu:
    parent: base
    xyz: [0, 0, 0]
    rpy: [0, 0, 0]
    accepts: [imu]
defaults:
  lidar:
    - variant: arena_default
      mount: lidar
  imu:
    - variant: arena_default
      mount: imu
